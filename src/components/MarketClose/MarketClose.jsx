import React from "react";
import marketCloseGIP from "./close.gif";

const MarketClose = () => {
  return <img src={marketCloseGIP} alt="MarketClose.." className="d-block m-auto" />;
};

export default MarketClose;
